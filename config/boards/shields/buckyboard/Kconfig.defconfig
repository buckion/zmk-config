if SHIELD_BUCKYBOARD_LEFT

config ZMK_KEYBOARD_NAME
    default "Buckyboard"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_BUCKYBOARD_LEFT || SHIELD_BUCKYBOARD_RIGHT

config ZMK_SPLIT
    default y

endif
